---
// CardGrid.astro
const items = [
    {
        image: { url: '/images/kerzenGlas.jpg', alt: 'kerzen glas' },
        title: 'Geplottete Gläser',
    },
    {
        image: { url: '/images/newsAbwasch.png', alt: 'tuch' },
        title: 'Abwaschtücher',
    },
    {
        image: { url: '/images/spices.png', alt: 'gewürze' },
        title: 'Gewürze von Streuwerk',
    }
];

// get images from wp backend clientside

//const pageId = 10;
//const res = await fetch(`http://localhost:10003/wp-json/wp/v2/pages/${pageId}`);
//const page = await res.json();
---

<section class="mainWrapper contentReveal">
    <h2 class="text-4xl font-bold text-center mb-10">Unsere Neuheiten</h2>
    <div class="flex gap-5 flex-col lg:flex-row">
        {items.slice(0, 6).map((item, index) => (
                <div class="relative flex flex-col rounded-lg overflow-hidden shadow w-full lg:w-[33.33333%]" key={index}>
                    <img
                            src={item.image.url}
                            alt={item.image.alt || ''}
                            class="w-full object-cover transition-transform duration-300 scale-105"
                    />
                    <div class="absolute bottom-0 left-0 h-1/3 bg-[rgba(0,0,0,0.5)] flex flex-col gap-2 flex justify-center items-center w-full">
                        <h3 class="text-2xl font-semibold text-white!">{item.title}</h3>
                    </div>
                </div>
        ))}
    </div>
</section>
